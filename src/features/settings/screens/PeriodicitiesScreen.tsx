import { Screen } from "@/shared/ui/layout/Screen";

export function PeriodicitiesScreen(): React.JSX.Element {
  return (
    <Screen
      subtitle="Configura las periodicidades que podran usarse en la creacion y gestion de creditos."
      title="Periodicidad"
    />
  );
}
