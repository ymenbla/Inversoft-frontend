import { Screen } from "@/shared/ui/layout/Screen";

export function PaymentMethodsScreen(): React.JSX.Element {
  return (
    <Screen
      subtitle="Administra los metodos de pago disponibles para los procesos operativos."
      title="Metodos de pagos"
    />
  );
}
